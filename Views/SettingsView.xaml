<Page
    x:Class="Launcher.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Launcher.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Загальні Налаштування Лаунчера" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold"/>
                <TextBlock Text="Встановіть параметри поведінки за замовчуванням, шлях до Java Runtime та опції збереження" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
            </StackPanel>
        </Grid>

        <!-- Content Forms -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <StackPanel Spacing="16">
                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Анімації та ефекти" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>

                            <ToggleSwitch Header="Увімкнути анімації" x:Name="AnimToggle" Toggled="AnimToggle_Toggled"/>

                            <StackPanel Spacing="8">
                                <TextBlock Text="Швидкість анімацій" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                <Slider x:Name="AnimSpeedSlider" Minimum="0.25" Maximum="3.0" StepFrequency="0.25" ValueChanged="AnimSpeedSlider_ValueChanged"/>
                            </StackPanel>

                            <ComboBox Header="Glow" x:Name="GlowCombo" SelectionChanged="GlowCombo_SelectionChanged" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Off" Tag="Off"/>
                                <ComboBoxItem Content="Low" Tag="Low"/>
                                <ComboBoxItem Content="Medium" Tag="Medium"/>
                                <ComboBoxItem Content="High" Tag="High"/>
                                <ComboBoxItem Content="Ultra" Tag="Ultra"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Директорії та Шляхи" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                            
                            <StackPanel Spacing="6">
                                <TextBlock Text="Директорія гри (.minecraft)" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="C:\Users\Admin\AppData\Roaming\.minecraft" x:Name="GamePathText"/>
                                    <Button Grid.Column="1" Content="Огляд..." Click="Browse_Click"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Spacing="6">
                                <TextBlock Text="Шлях до Java Executable" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="C:\Program Files\Eclipse Adoptium\jdk-17.0.8.7-hotspot\bin\javaw.exe" x:Name="JavaPathText"/>
                                    <Button Grid.Column="1" Content="Автопошук" Click="AutoDetectJava_Click"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Регіональні Налаштування" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                            
                            <StackPanel Spacing="6">
                                <TextBlock Text="Мова інтерфейсу" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                <ComboBox HorizontalAlignment="Stretch" SelectedIndex="0">
                                    <ComboBoxItem Content="Українська (Ukrainian)"/>
                                    <ComboBoxItem Content="English (US)"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Right Column -->
                <StackPanel Grid.Column="1" Spacing="16">
                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Поведінка за замовчуванням" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                            
                            <ToggleSwitch Header="Залишати лаунчер відкритим після запуску гри" IsOn="True"/>
                            <ToggleSwitch Header="Автоматично перевіряти оновлення при запуску" IsOn="True"/>
                            <ToggleSwitch Header="Надсилати анонімні логи телеметрії та краш-репорти" IsOn="False"/>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Навігація" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                            <TextBlock Text="Приховуйте вкладки, змінюйте порядок і позначайте улюблені" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap"/>

                            <ComboBox Header="Положення панелі" x:Name="NavPositionCombo" SelectionChanged="NavPositionCombo_SelectionChanged" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Left" Tag="Left"/>
                                <ComboBoxItem Content="Right" Tag="Right"/>
                                <ComboBoxItem Content="Top" Tag="Top"/>
                                <ComboBoxItem Content="Bottom" Tag="Bottom"/>
                            </ComboBox>

                            <ListView x:Name="NavItemsList" ItemsSource="{x:Bind NavService.Items}" SelectionMode="None" MaxHeight="360">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:NavigationItemSettings">
                                        <Grid ColumnSpacing="8" Padding="0,4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center"/>
                                            <Button Grid.Column="1" Content="⭐" Padding="6,2" Background="Transparent" BorderThickness="0"
                                                    Tag="{x:Bind Id}" Click="FavoriteNav_Click"/>
                                            <Button Grid.Column="2" Content="&#x2191;" Padding="6,2" Background="Transparent" BorderThickness="0" Tag="{x:Bind Id}" Click="MoveNavUp_Click"/>
                                            <Button Grid.Column="3" Content="&#x2193;" Padding="6,2" Background="Transparent" BorderThickness="0" Tag="{x:Bind Id}" Click="MoveNavDown_Click"/>
                                            <ToggleSwitch Grid.Column="4" IsOn="{x:Bind IsVisible, Mode=OneTime}" Tag="{x:Bind Id}" Toggled="VisibleNav_Toggled" OnContent="" OffContent=""/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Очищення та Діагностика" FontWeight="Bold" Style="{ThemeResource BodyTextBlockStyle}"/>
                            <TextBlock Text="Якщо у вас виникають труднощі із завантаженням версій чи запуском клієнта, очистіть локальний кеш метаданих." FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap"/>
                            
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Content="Очистити Кеш" Foreground="Orange" HorizontalAlignment="Stretch" Click="ClearCache_Click"/>
                                <Button Grid.Column="1" Content="Скинути Лаунчер" Foreground="Red" HorizontalAlignment="Stretch" Click="ResetLauncher_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Button Content="Зберегти Всі Налаштування" Style="{ThemeResource AccentButtonStyle}" Click="SaveSettings_Click" HorizontalAlignment="Right" Margin="0,12,0,0"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
