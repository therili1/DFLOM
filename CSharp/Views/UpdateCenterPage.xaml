<Page
    x:Class="Launcher.Views.UpdateCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Launcher.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Центр Оновлень Лаунчера" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold"/>
                <TextBlock Text="Сторінка релізів лаунчера на GitHub" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Spacing="4" HorizontalAlignment="Center">
                <TextBlock Text="Поточна версія" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind ViewModel.CurrentVersion, Mode=OneWay}" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" HorizontalAlignment="Center"/>
            </StackPanel>

            <TextBlock Text="Всі релізи, зміни та останні збірки лаунчера публікуються на GitHub." TextWrapping="Wrap" Opacity="0.85" HorizontalAlignment="Center" MaxWidth="360" TextAlignment="Center"/>

            <Button Content="Відкрити сторінку релізів на GitHub" Click="OpenReleasesPage_Click" Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Center" Height="40" Padding="20,0"/>
        </StackPanel>
    </Grid>
</Page>
