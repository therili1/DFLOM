<Page
    x:Class="Launcher.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Launcher.Models"
    xmlns:viewmodels="using:Launcher.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel with User Profile -->
        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Text="Лаунчер Minecraft" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="Bold"/>
                <TextBlock Text="Ласкаво просимо до кастомного лаунчера нового покоління" Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
            </StackPanel>

            <!-- Authentication Controls -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <Button VerticalAlignment="Center" Visibility="{x:Bind ViewModel.IsAuthenticated, Mode=OneWay}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{x:Bind ViewModel.Username, Mode=OneWay}" FontWeight="SemiBold" FontSize="16"/>
                        <FontIcon Glyph="&#xE70D;" FontSize="10" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.Flyout>
                        <Flyout Placement="Bottom">
                            <StackPanel Spacing="4" Width="240">
                                <TextBlock Text="Збережені акаунти" FontWeight="Bold" FontSize="12" Opacity="0.7" Margin="0,0,0,4"/>
                                <ListView ItemsSource="{x:Bind ViewModel.SavedAccounts, Mode=OneWay}" SelectionMode="None" MaxHeight="220">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:SavedAccount">
                                            <Grid ColumnSpacing="8" Padding="4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal" Spacing="8" Tapped="AccountRow_Tapped" Tag="{x:Bind}">
                                                    <TextBlock Text="👤" FontSize="16" VerticalAlignment="Center"/>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <TextBlock Text="{x:Bind Username}" FontWeight="SemiBold" FontSize="13"/>
                                                        <TextBlock Text="{x:Bind AuthType}" FontSize="10" Opacity="0.6"/>
                                                    </StackPanel>
                                                </StackPanel>
                                                <Button Grid.Column="1" Content="✕" FontSize="10" Padding="6,2" Click="RemoveAccount_Click" Tag="{x:Bind}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                <Button Content="+ Додати акаунт" HorizontalAlignment="Stretch" Margin="0,4,0,0" Click="ShowLoginDialog_Click"/>
                                <Button Content="Вийти з поточного" HorizontalAlignment="Stretch" Click="Logout_Click"/>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <Button Content="Увійти" Click="ShowLoginDialog_Click" Style="{ThemeResource AccentButtonStyle}" Visibility="{x:Bind ViewModel.IsAuthenticated, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}"/>
            </StackPanel>
        </Grid>

        <!-- Main Dashboard Split -->
        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Play Center -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="24" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <Grid RowSpacing="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Installation Selector -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="Виберіть Профіль (Клієнт)" FontWeight="SemiBold" Style="{ThemeResource BodyTextBlockStyle}"/>
                        <ComboBox HorizontalAlignment="Stretch" ItemsSource="{x:Bind ViewModel.Instances, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedInstance, Mode=TwoWay}" DisplayMemberPath="Name"/>
                    </StackPanel>

                    <!-- Profile Info Area -->
                    <StackPanel Grid.Row="1" Spacing="12" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🎮" FontSize="64" HorizontalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.SelectedInstance.Name, Mode=OneWay}" FontWeight="Bold" FontSize="20" HorizontalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                            <TextBlock Text="Версія:" Opacity="0.7"/>
                            <TextBlock Text="{x:Bind ViewModel.SelectedInstance.Version, Mode=OneWay}" FontWeight="SemiBold"/>
                            <TextBlock Text="|" Opacity="0.5"/>
                            <TextBlock Text="Двигун:" Opacity="0.7"/>
                            <TextBlock Text="{x:Bind ViewModel.SelectedInstance.Loader, Mode=OneWay}" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Big Play Button and Status -->
                    <StackPanel Grid.Row="2" Spacing="12">
                        <!-- Progress info during launching -->
                        <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.IsLaunching, Mode=OneWay}">
                            <ProgressBar Minimum="0" Maximum="100" Value="{x:Bind ViewModel.LaunchProgress, Mode=OneWay}" Height="12" CornerRadius="6" HorizontalAlignment="Stretch"/>
                            <TextBlock Text="{x:Bind ViewModel.LaunchStatus, Mode=OneWay}" FontSize="12" HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>
                        </StackPanel>

                        <!-- Static status when not launching -->
                        <TextBlock Text="{x:Bind ViewModel.LaunchStatus, Mode=OneWay}" FontSize="12" HorizontalAlignment="Center" Opacity="0.7" Visibility="{x:Bind ViewModel.IsLaunching, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}"/>

                        <Button Content="ГРАТИ" Style="{ThemeResource AccentButtonStyle}" FontSize="24" FontWeight="Bold" Height="64" HorizontalAlignment="Stretch" Click="Play_Click" IsEnabled="{x:Bind ViewModel.IsLaunching, Converter={StaticResource InverseBoolConverter}, Mode=OneWay}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right: News and Community Feed -->
            <StackPanel Grid.Column="1" Spacing="16">
                <TextBlock Text="Останні Новини" Style="{ThemeResource SubtitleTextBlockStyle}" FontWeight="Bold"/>
                
                <ListView ItemsSource="{x:Bind ViewModel.NewsFeed, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="x:String">
                            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="{x:Bind}" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}" LineHeight="18"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
